#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints _putchar followed
 * by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *ptc = "_putchar";

	putchar(ptc);
	putchar("\n");

	return (0);
}
